import React from 'react';
import ReactDOM from 'react-dom/client';
import CoastFireMatrix from './CoastFireMatrix.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CoastFireMatrix />
  </React.StrictMode>
);
